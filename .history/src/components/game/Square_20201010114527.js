import React from 'react';

const Square = () => {
  const determineRow = () => {};

  return (
    <div className={`${determineRow}`}>
      <div className="square"></div>
    </div>
  );
};

export default Square;
